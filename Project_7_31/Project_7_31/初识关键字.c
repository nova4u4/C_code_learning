#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

// c语言本身内置的 不是自己创建的 关键字
// auto——常省略 局部变量(auto) int a 出作用域自动销毁   
// switch  case  default      if else   goto  
// enum（枚举） struct（结构体） union（联合体）  signed（有符号的） unsighed（无符号的）
// continue break     for  while   do while   
// const 常属性   extern 声明外部符号的  register 寄存器 static 静态的   return 
// void（函数返回类型 参数 无）  volatile 

int main05()
{
	// 变量的命名要有意义 是数字字母下划线 但不能以数字开头 不能和关键字相同
	return 0;
 }